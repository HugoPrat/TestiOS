# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'NewProject - Shared Code' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NewProject - Shared Code
  pod 'LibTorch-Lite', '~> 1.10.0'

end

target 'NewProject - Standalone Plugin' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NewProject - Standalone Plugin
  pod 'LibTorch-Lite', '~> 1.10.0'

end
